language: python
python:
  - "3.5"
  - "3.4"
  - "2.7"

install:
  - "pip install -r requirements.txt"
  - "pip install requests-mock"
  - "pip install mock"
  - "pip install coveralls"

script: "py.test ."

after_success:
  - coveralls
